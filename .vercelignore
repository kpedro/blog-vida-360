# post.html existe no Git para o build (npm run build → api/post-template.html), mas NÃO pode ir para o artefacto estático:
# se existir ./post.html no deploy, a CDN serve esse ficheiro ANTES do rewrite → WhatsApp/Facebook veem OG genérico (banner).
# Manter aqui garante que só /api/dynamic-post devolve og:image por artigo.
post.html
